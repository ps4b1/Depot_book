<%content_for :cart do  %>
  <% if @cart && @cart.line_items.any?  %>
    <%= render partial: 'carts/cart', locals: {delete_last_redirect: true, cart: @cart}  %>
  <% end %>
<% end %>

<section class="depot_form">
  <h1><%= t('.legend') %></h1>
  <%= render 'form', order: @order %>
</section>
<%= javascript_pack_tag('pay_type') %>